# Backups gerados pela ferramenta (DEC-006) — nunca versionar.
backups/

# Artefatos gerados ao rodar a demo/self-test dentro do repo — nunca versionar.
# A demo.yaml CRIA estes via create_file; se vazarem para examples/, os testes
# de GUI/self-test (que copiam demo_project) quebram com "health.py já existe".
examples/demo_project/src/health.py
# Sandboxes criadas por `apply --sandbox` (pasta irmã *_sandbox_<timestamp>).
*_sandbox_*/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Ambientes virtuais
.venv/
venv/
env/

# Testes / cobertura
.pytest_cache/
.coverage
htmlcov/

# Ferramentas
.ruff_cache/
.mypy_cache/

# IDE / SO
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Build do executável (F3)
*.spec
