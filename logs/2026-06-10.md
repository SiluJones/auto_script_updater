# Log — 2026-06-10

## Objetivo da sessão
Analisar os logs de console enviados (problemas de ativação/teste), a lista de
arquivos e o manifest para entender a estrutura real no disco, diagnosticar a
fricção de primeiro uso e prosseguir com correções e ensinamentos.

## Feito
- **Diagnóstico dos consoles:**
  - `console-260608_-_2.txt`: `python -m pytest` → 42 passed. A ferramenta está instalada e funcional no ambiente do usuário.
  - `console-260608.txt`: `python -m src validate instrucao.yaml` → "Arquivo não encontrado". **Não é bug** — o comando do README usava `instrucao.yaml` como *placeholder genérico*, e o usuário não tinha um arquivo com esse nome. A ferramenta reportou o erro corretamente.
- **Estrutura entendida (lista + manifest):** o projeto no disco vive em `C:\Users\alexk\Arquiteturas\ASU\auto_script_updater`; os docs de contexto ficam em `meta/` (não na raiz) e os logs em `logs/`. O código bate 100% com o entregue na F1 (nomes incluídos). Os arquivos do Projeto são um *flatdrop* (achatados) do disco real, gerado 2026-06-10.
- **Causa raiz (onboarding, não bug):** faltava um caminho "copie-cole-e-funciona" para o primeiro teste. O `examples/exemplo_instrucao.yaml` é ilustrativo e **não executável** (aponta para `src/auth/login.py`, `C:\projetos\...`, etc., que não existem).
- **Demo executável criada:** `examples/demo_project/` (arquivos reais: `src/calculator.py`, `README.md`, `config.json`, `web/app.js`) + `examples/demo.yaml`. Cobre os 4 tipos + `create_file`, todo com `path_mode: relative`. Testada ponta a ponta no container: `validate` → `apply --dry-run` → `apply` → `rollback`, com restauração total confirmada.
- **README corrigido:** nova seção **Quickstart** (comandos executáveis com a demo) + seção "Uso no seu projeto" com placeholder explícito (`MINHA_INSTRUCAO.yaml`) + nota de que o `exemplo_instrucao.yaml` é só ilustrativo.
- **Testes:** 43 verdes (42 + 1 novo para a guarda; ver FIX-001).

## Decisões
- *(sem novas DEC — decisões de arquitetura inalteradas)*

## Bugs
- **FIX-001** (registrado em DECISIONS.md): `replace_context_block` produzia **corrupção silenciosa** quando o `new_content` incluía as próprias âncoras `before`/`after` (elas permanecem no arquivo, então eram duplicadas) — e o `apply` "funcionava" sem erro. Adicionada guarda que detecta a assinatura inequívoca (primeira linha == `before` e última == `after`) e lança erro didático. Os dois exemplos (`exemplo_instrucao.yaml` e `demo.yaml`) tinham esse defeito e foram corrigidos (new_content = só o miolo).

## Aprendizados / armadilhas
- **Corrupção silenciosa é o pior modo de falha** numa ferramenta de patch: um resultado errado que não levanta erro é mais perigoso que uma falha barulhenta. Toda estratégia deve falhar alto quando o resultado provável é inválido.
- **Convenção das âncoras de contexto:** em `replace_context_block`, `before`/`after` são CONTEXTO e permanecem no arquivo; o `new_content` é apenas o **miolo** entre elas. Incluir as âncoras duplica — daí a guarda. (Decisão de manter a convenção A, alinhada ao *apply_patch*, em vez de trocar a semântica.)
- **Indentação de block scalar YAML:** `|` remove a indentação comum do bloco; para preservar N espaços de indent no conteúdo use o indicador explícito (ex.: `|2`). A demo usa `|2` no bloco JS para manter os 2 espaços.
- **O dry-run paga por si:** foi o `apply --dry-run` da demo que expôs a duplicação antes de qualquer escrita. Reforça o valor de sempre revisar o diff antes de aplicar.

## Onde parei
F1 robustecida (guarda anti-corrupção + 43 testes) e **onboarding resolvido**
(demo executável + README com Quickstart). O bundle entregue
(`auto_script_updater.zip`) já inclui a demo e as correções.

## Próximos passos
- [ ] F2: iniciar a GUI PySide6 (`src/gui/main_window.py`) reusando a mesma pilha; indicador de confiança 🟢/🟡/🔴 derivado do dry-run por modificação.
- [ ] Polir F1 (opcional): rodar `ruff`/`black`; fixtures de borda (cp1252, CRLF, ocorrência múltipla, contexto ambíguo).
- [ ] Avaliar um canal de *aviso* (warning) no engine para casos suspeitos que não justificam erro fatal (ver IDEAS).
- [ ] Avaliar flag opcional `include_anchors` no `replace_context_block` (ver IDEAS), caso o uso real peça substituir o bloco com as âncoras.

---

# Parte 2 (mesmo dia) — Polimento da F1 + caça a erros silenciosos

## Objetivo
Polir a F1 (ruff/black + bordas), **provar multilinguagem** (C#, C++, Java, JSX, TSX, GDScript) e auditar todas as estratégias atrás de outros erros silenciosos, a pedido do usuário (que usará a ferramenta como beta tester em vários projetos).

## Feito
- **Auditoria estratégia por estratégia** (text, python, json, engine de leitura/escrita) com três achados graves, todos corrigidos com teste:
  - **DEC-011 — unicidade implícita:** localizador ambíguo sem `occurrence` aplicava na 1ª ocorrência em silêncio (Armadilha #5 estava documentada mas nunca implementada). Agora: `occurrence` ausente exige match único (erro didático com contagem); `occurrence` presente = posicional explícito. Implementado em `_resolve_occurrence` e usado por insert_*/replace_line/context_block(before); `replace_section` rejeita heading duplicado.
  - **FIX-002 — encodings:** BOM UTF-8 (Visual Studio/.cs) virava `\ufeff` invisível que quebrava localizadores da 1ª linha → agora `utf-8-sig` com BOM **preservado** no roundtrip. UTF-16/32 "decodificável" pelo fallback cp1252 como lixo (e `replace_file` converteria encoding em silêncio) → BOMs detectados e rejeitados com erro claro. Newline passou a ser detectado no texto decodificado. Bônus: erro de leitura/decodificação agora entra no fluxo de falha por arquivo (antes a exceção escapava do engine — o teste novo pegou isso).
  - **FIX-003 — fences no markdown:** `## X` dentro de ```/~~~ era tratado como heading (achava seção errada ou cortava a real no meio). `replace_section` agora é fence-aware na busca e no fim de seção.
- **Semânticas fixadas em teste (documentadas, não mudadas):** decorador não repetido no `new_content` é removido junto (nó completo — docstring atualizada); `after` do context block fecha no **primeiro** match (chaves aninhadas pedem âncora distintiva como `"\n}"` — docstring atualizada e teste demonstra os dois caminhos).
- **Multilinguagem provada:** `tests/test_multilang.py` com C#, C++, Java, JSX, TSX e **GDScript** (tabs preservados), incluindo engine completo com C#+BOM e rejeição de UTF-16.
- **Bordas do backlog cobertas:** cp1252 roundtrip real, CRLF preservado, arquivo sem newline final, ocorrência múltipla, contexto ambíguo.
- **Qualidade:** `ruff check` limpo (19 auto-fix + B904 manuais com `raise ... from exc`) e `black` aplicado; suíte re-validada após formatação.
- **Higiene de versão:** `src/__init__.py` e `pyproject.toml` estavam em 0.0.1 (dessincronizados do STATUS) → unificados em **0.2.0** (minor por mudança de comportamento da DEC-011).
- **Testes: 43 → 68**, todos verdes; smoke da demo (apply+rollback) re-executado pós-black.

## Decisões
- **DEC-011** (unicidade implícita de localizadores) — ver DECISIONS.md.

## Bugs
- **FIX-002** (BOM/UTF-16 + leitura sem captura no engine) e **FIX-003** (fences) — ver DECISIONS.md.

## Aprendizados / armadilhas
- *Armadilha documentada não é armadilha tratada*: a #5 (unicidade) estava no CONTEXT desde a F0 e nunca tinha virado código. Auditorias devem cruzar docs ↔ implementação.
- O fallback permissivo de encoding (cp1252 aceita quase tudo) precisa de **portões de BOM** na frente, ou transforma binário/UTF-16 em "texto" plausível.
- `black` reformatou ~todos os arquivos; rodar a suíte **depois** da formatação é obrigatório (passou intacta).

## Onde parei
F1 polida e endurecida (v0.2.0): 68 testes, lint/format limpos, multilinguagem comprovada. Pendência priorizada pelo usuário: **kit de ensino para a IA geradora** (INSTRUCTION_GUIDE + bloco de prompt) — registrado em IDEAS e no backlog do STATUS como pré-requisito para usar a ferramenta nos demais projetos. Depois: F2 (GUI).
