# Log — 2026-06-14

## Objetivo da sessão
Responder à dúvida do usuário sobre canais de feedback (Kit × ASU) e diagnosticar o novo `console-260614.txt` — verificar, via `_MANIFEST.md`, se os arquivos foram colocados nos lugares certos e se faltou algo no processo.

## Feito
- **Diagnóstico do `console-260614.txt`:** 4 testes de GUI + self-test falhando. Confirmado pelo manifest (gerado 2026-06-14 11:25) que o usuário JÁ aplicou a v0.5.1 e subiu tudo nos lugares CERTOS — os arquivos `backup_manager.py`, `patch_engine.py` e `main_window.py` subidos são byte a byte idênticos aos do container (que passam). Logo, não era bug de código nem erro de processo do usuário.
- **Causa raiz (FIX-009):** a `demo.yaml` cria `examples/demo_project/src/health.py` via `create_file`; numa execução anterior (provavelmente antes do FIX-008, quando o rollback no Windows não removia o criado) o arquivo ficou como RESÍDUO em `examples/demo_project/src/` e foi versionado/subido (consta no manifest). Testes de GUI e self-test copiam `demo_project` com `copytree` → resíduo vem junto → dry-run encontra `health.py` já presente → `assert not exists()` falha; self-test idem. Confirmado: o `health.py` subido é byte a byte o output da demo.
- **Reprodução + correção provadas:** com o resíduo presente, reproduzi o erro EXATO do usuário (`assert not True` na linha do health.py); com a correção, 90 verdes + self-test OK. Correção em 3 camadas (FIX-009): (1) `.gitignore` ignora `examples/demo_project/src/health.py` e `*_sandbox_*/`; (2) fixture `demo_root` e (3) self-test limpam artefatos gerados pela demo após o copytree (defesa em profundidade); resíduo removido do pacote.
- **Pergunta de design respondida (DEC-017):** feedback do KIT (princípios/templates/regras do Kit de Contexto) → IDEAS › «Feedback para o Kit»; feedback do ASU (a ferramenta) → DEC/FIX/IDEAS/STATUS normais. São canais distintos; o ASU não precisa de canal paralelo porque já é o objeto do projeto. Registrado também na própria seção «Feedback para o Kit».
- Versão **0.5.2**; **90 testes verdes**; ruff/black limpos; bundle `asu_v0.5.2.zip` validado por extração + pytest, agora SEM o `health.py` residual.

## Decisões
- **DEC-017** — dois canais de feedback distintos (Kit × ASU). Ver DECISIONS.md.

## Bugs
- **FIX-009** — resíduo da demo (`health.py`) versionado quebrava 4 testes de GUI + self-test no Windows. Ver DECISIONS.md.

## Aprendizados / armadilhas
- **"Arquivos idênticos mas testes falham" = estado do ambiente, não código.** Aqui o estado era um arquivo residual versionado; noutro caso seria cache/caminho. Comparar os arquivos subidos com os íntegros (diff) foi o que isolou o problema rápido.
- **Demo que escreve no próprio repo precisa de `.gitignore` + limpeza nos testes.** Senão o output vira resíduo versionado que quebra quem clona. (Feedback ao Kit registrado.)
- **O manifest é a prova de "lugar certo".** Bastou cruzá-lo para descartar erro de processo do usuário e focar no resíduo.

## Onde parei
v0.5.2 fechada: resíduo da demo corrigido e blindado, canais de feedback esclarecidos. Entregue em `asu_v0.5.2.zip` (limpo) + docs. Próximo passo do usuário: aplicar v0.5.2, rodar `python -m pytest` (deve dar 90 passed e self-test OK).

## Próximos passos
- [ ] Usuário: aplicar v0.5.2 no Windows; confirmar 90 passed + self-test OK.
- [ ] Usuário: ao re-subir o projeto, conferir que `examples/demo_project/src/` tem só `calculator.py` (sem `health.py`).
- [ ] Testar o kit v2 num projeto consumidor real; primeiro projeto médio via `apply --sandbox`.
- [ ] Avaliar CI Windows e checklist do Kit para demos que geram arquivos (feedback ao Kit em IDEAS).
