# Log — 2026-07-30

## Objetivo da sessão
Receber o **template-update do KCM v1.94.0** (19 arquivos `__template-update` + `_UPDATE-MANIFEST.md`), comparar contra o vivo sem aplicar nada, reportar (a) novidade útil, (b) choque e (c) o que o projeto tem e o template não cobre — e, depois da decisão do usuário, executar os merges. Sessão de PROCESSO: nenhuma mudança de código prevista.

## Feito
- **Estado lido antes de comparar** (regra nova do próprio pacote): mount listado (59 arquivos do FlatDrop + 5 notas + 19 do pacote), versão **0.9.2**, 158 testes, repouso de maturação. Commit não era legível pelo mount (cópia achatada não tem `.git`); pedido uma vez e informado pelo usuário: **`95900aa`** ("ultimas specs").
- **Comparação completa** dos 19 arquivos do pacote contra o vivo, com diff real (não de memória): CEREBRO (55k vs 34k), INSTRUCOES, CLAUDE.md, `settings.json`, as duas skills, os 10 modelos de `meta/`, `_TEMPLATE.md` de WO, `.gitignore`, `.flatdropignore`. Confirmado por diff que **`LOG-TEMPLATE.md` é byte-a-byte idêntico** ao do kit.
- **Confirmado o formato descontinuado:** o `_MANIFEST` do FlatDrop mostra `.claude/commands/apply-spec.md` e `.claude/commands/wrap.md` — migração obrigatória para `.claude/skills/`.
- **Entregues como arquivo inteiro (chat):** `meta/CEREBRO.md` (merge), `CLAUDE.md`, `.flatdropignore.txt`, `meta/SPEC.md` (novo), `meta/workorders/_TEMPLATE.md` (novo), `.claude/skills/apply-wo/SKILL.md` (novo), `.claude/skills/wrap/SKILL.md` (novo), `INSTRUCOES-DO-PROJETO.md` (painel) e este log.
- **Entregue como WO:** `260730-wo0014-migracao-vocabulario-wo.md` — o `git mv`, a remoção de `.claude/commands/` e os deltas em CONTEXT/STATUS/DECISIONS/CHANGELOG/IDEAS/GLOSSARY. Canal dos meta neste ciclo = **CODE** (a WO é o registro; o chat não entrega esses seis docs inteiros).
- **CEREBRO mergeado** preservando o que o projeto evoluiu (convenção de nome das instruções ASU `AAMMDD-asuNNNN.yaml`, escopo das raias CEREBRO×CLAUDE.md, nota «Saída de código deste projeto») e adotando o que faltava: «Técnicas específicas deste projeto», «Análise antes do compromisso», «Ao receber um template-update do KCM», «Bloco de fecho de turno», «Refino das Instruções», 3 regras de higiene novas, caudas dos princípios 1/8/10/11, linha do `DECISIONS-archive.md` na tabela de docs.
- **«Técnicas específicas deste projeto» preenchida** com 7 armadilhas reais que estavam só no DECISIONS: fixture de `QSettings`, âncora multi-linha × CRLF, `"%~dp0."`, `health.py` da demo, backup derivado da raiz + MAX_PATH, mount sem `.git`, e a salvaguarda de conferir se a WO já existe.
- **Painel medido e refinado:** 6.172 → **7.255** caracteres (teto 7.450 com o modo Code ligado). Cortes feitos nas descrições redundantes com o CEREBRO, não em processo.

## Decisões
- **DEC-033** (nova, na WO): vocabulário WO × spec — `git mv meta/specs meta/workorders`, «spec» passa à spec de feature, numeração contínua em `wo0014`, sinalização única no CONTEXT, migração das skills, `.flatdropignore` em formato de bloco, CEREBRO mergeado. Decisão do usuário, alinhada aos demais projetos do KCM; o chat havia recomendado o oposto (manter o vocabulário do ASU) e foi vencido por um argumento melhor: divergência de vocabulário com o kit é dívida permanente.
- **DEC-021 e DEC-031 receberam marcador** (`>` de vocabulário atualizado / refinada), seguindo a convenção que a DEC-020 já usava.
- **Mantido deliberadamente:** os nomes dos 13 arquivos históricos, o `.txt` no `.flatdropignore.txt`, e o `.gitignore` do ASU (o do kit é um 5-linhas genérico; o daqui cobre `zz_backups`, sandboxes, o `health.py` da demo e `*.spec`).
- **`meta/specs/` e `meta/analises/` NÃO foram criadas vazias** — nascem no primeiro uso, como o CEREBRO manda.

## Bugs
- Nenhum bug de produto. Nenhuma linha de `src/` ou `tests/` foi tocada.

## Aprendizados / armadilhas
- **Medir caracteres, não bytes.** A primeira medição do painel deu 6.408 «caracteres» que eram BYTES; em PT-BR são 236 acentos de diferença — o painel tinha 6.172 caracteres. Perto de um teto de 6.900/7.450 isso é material. Vale para qualquer medição de orçamento de instrução.
- **O HUB já não existia aqui.** A mensagem do KCM de 07-29 pedia higiene («apaguem a cópia de vocês»), mas a DEC-029 (07-15) tinha antecipado — nada a fazer. Confirmar antes de executar pendência vinda de fora custou uma leitura e evitou um commit vazio.
- **Referência histórica não se corrige; sinaliza-se.** Varrer `meta/specs/` em CHANGELOG/ROADMAP/DECs antigas produziria um diff grande em texto datado. Uma nota no CONTEXT resolve a navegação — e é a única fonte dessa tradução, o que a torna frágil se alguém a remover.
- **O pacote de update tem de ficar no mount até o merge fechar.** Se sair antes, vai embora o original de comparação. Regra que veio no próprio pacote e valeu nesta sessão.

## Onde parei
Todos os arquivos da sessão entregues; **nada aplicado ao repo ainda** — aplicar é decisão do usuário. O repo segue em **0.9.2 / `95900aa` / 158 testes verdes**, em repouso de maturação. O próximo gesto é mecânico: mover os arquivos entregues, rodar `/apply-wo 260730-wo0014-migracao-vocabulario-wo.md` no Claude Code e commitar. As notas `260720-1502.txt` e `260720-1542.txt` podem sair do mount (absorvidas); `260723-1748.txt` e as duas notas do KCM de 07-29 só saem depois que a wo0014 for aplicada, porque é ela que as captura no IDEAS.

## Próximos passos
- Aplicar a **wo0014** no Claude Code e conferir o checklist dela (em especial: o rename aparecer como *rename* no git, e as duas skills aparecerem no `/`).
- Depois disso, arquivar as 5 notas `.txt`/`.md` do mount e regerar o pacote do FlatDrop (o novo `.flatdropignore` já esconde `meta/workorders/*` e reinclui o `_TEMPLATE.md`).
- **Dívida nº 1, inalterada:** `README.md` e `GUIA_PASSO_A_PASSO.md` estão em 0.8.5 e precisam ir a 0.9.2 (syntax-highlight 0.8.6, ressalva 🟡 no CLI 0.8.7, `zz_backups` + campo Backup derivado 0.9.0, atalho «abrir GUI» 0.9.1/0.9.2). Captura de produção de 07-17 disponível para embutir.
- Item de produto entrando na fila (capturado hoje no IDEAS): gravar **qual instrução gerou o backup** no `history.log`/`manifest.txt` — candidato natural à primeira **spec de feature** em `meta/specs/`, com critério de aceite para o fallback de manifesto legado.
