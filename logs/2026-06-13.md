# Log — 2026-06-13

## Objetivo da sessão
Integrar a atualização do Kit de Contexto (10 `__update`) ao projeto, usando o `_MANIFEST.md` para mapear nomes reais; diagnosticar a situação relatada nos arquivos `260613-console.txt` e `ideia-260613.txt` (dizer ao usuário o que ele fez de errado), e pesquisar/refinar as ideias dele e corrigir os bugs.

## Feito
- **Mapeamento via `_MANIFEST.md`:** confirmado que os docs de contexto vivem em `meta/` (FlatDrop, separador `__`). Novos arquivos do kit no projeto: `CLAUDE.md`, `LOG-TEMPLATE.md`, `HISTORICO.md`, `README__meta.md`.
- **Diagnóstico do console (não foi erro do usuário):** o `python -m pytest` dava **5 failed, 85 passed** e o `self-test` falhava — tudo causado por um único bug de Windows (FIX-008). Os outros itens do console eram triviais e esperados: `validate instrucao.yaml` falhou porque o arquivo não existia (era exemplo), e `apply ... --root` sem valor foi erro de digitação de argumento.
- **FIX-008 (bug crítico, Windows) corrigido:** reescrito `src/core/backup_manager.py` — o espelho de backup era o caminho ABSOLUTO inteiro recriado sob `backups/`, estourando o MAX_PATH (260 chars) no Windows; agora é RELATIVO à raiz (`BackupManager(root=...)`, `relative_to`), com fallback `_abs/<drive>/...` para arquivos fora da raiz, e `manifest.txt` gravando o espelho explícito (`estado<TAB>original<TAB>espelho`); formato antigo ainda legível. `patch_engine.py` passa a raiz ao manager. Provado com caminho profundo simulando Windows: espelho vira `backups/<ts>/cfg.json` (raso).
- **Pesquisa (ideia de verificação):** validada com força — agentes de código emitem "linguagem de conclusão" independentemente do estado real; verificação confiável é *outcome-based* (ler o disco, não a transcrição). Fontes: DEV/CrisisCore ("AI coding agents lie about their work"), ReVeal, TDAD (−70% regressões). Virou **DEC-016** + **§8 do INSTRUCTION_GUIDE** + item no PROMPT_IA. Ressalva incorporada: ler o arquivo, nunca perguntar "deu certo?".
- **Análise da ideia de "arquivo de relatório":** recomendado NÃO criar arquivo dedicado (seria 4ª fonte de verdade; o feedback já tem canais — conversa/DECISIONS/IDEAS). Registrado em IDEAS › «Feedback para o Kit».
- **Resposta sobre tipos de arquivo:** a ferramenta JÁ modifica .md/.txt/.css/.html/.gd etc. pelo mecanismo universal (contexto/regex); refinamento semântico por linguagem fica para quando o uso real pedir, uma família por vez (web > JVM/C-like > nicho). Registrado em IDEAS.
- **Kit de contexto adaptado:** `CLAUDE.md` alinhado ao `CLAUDE__update.md` (2 parágrafos novos: adaptar Instruções do Projeto a este projeto; criar doc ausente na 1ª necessidade). Cabeçalhos dos templates (CONTEXT, DECISIONS, STATUS, IDEAS, ROADMAP, GLOSSARY, HISTORICO) atualizados ao formato novo, **preservando o conteúdo do projeto** (os `__update` são templates em branco; LOG-TEMPLATE veio idêntico).
- **Docs de contexto atualizados:** DECISIONS (+FIX-008, +DEC-016), CHANGELOG (0.5.1), STATUS (0.5.1 + última sessão), IDEAS (2 ideias do usuário + seção «Feedback para o Kit»).
- Versão **0.5.1**; **90 testes verdes**; ruff/black limpos; bundle `asu_v0.5.1.zip` validado por extração + pytest.

## Decisões
- **DEC-016** — verificação pós-aplicação pela IA (outcome-based; ler o disco). Ver DECISIONS.md.

## Bugs
- **FIX-008** — backup estourava o MAX_PATH do Windows (260 chars), derrubando 5 testes + self-test; invisível no CI Linux. Ver DECISIONS.md.

## Aprendizados / armadilhas
- **Teste verde no Linux não cobre o limite de caminho do Windows.** Para um produto Windows-first desenvolvido em container Linux, casos sensíveis a caminho (backup/cópia/sandbox) precisam de caminhos curtos por princípio e, idealmente, um CI Windows. (Feedback ao kit registrado em IDEAS.)
- **A ideia do usuário estava certa, e a pesquisa refinou o "como":** verificar lendo o arquivo, não perguntando — senão não pega a discrepância sutil que o próprio usuário não notou.
- **"Mais um arquivo" nem sempre ajuda:** a ideia do relatório, por mais bem-intencionada, colidiria com a regra de uma-fonte-por-dado; o canal certo já existia.

## Onde parei
v0.5.1 fechada: bug de Windows corrigido (o que travava o usuário), guia com verificação pós-aplicação, kit de contexto integrado. Tudo entregue em `asu_v0.5.1.zip` + docs avulsos. Próximo passo do usuário: aplicar o zip por cima, rodar `python -m pytest` (deve dar 90 passed agora) e o `self-test`.

## Próximos passos
- [ ] Usuário: aplicar v0.5.1 no Windows e confirmar **90 passed** + self-test OK.
- [ ] Usuário: substituir o kit v2 (guia/prompt) no projeto consumidor e re-testar a geração.
- [ ] Primeiro projeto médio via `apply --sandbox`.
- [ ] Avaliar CI Windows (feedback ao kit) e, sob demanda, refinamento semântico por família de linguagem (tree-sitter), começando pela web.
