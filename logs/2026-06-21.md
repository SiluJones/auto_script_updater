# Log — 2026-06-21

## Objetivo da sessão
(1) Ler os `.txt` citados (`260621-Sugestões do projeto KCM (chat).txt`, `260619-ideias.txt`) e os demais `.txt` para ver se há algo útil ou se podem ser arquivados. (2) Analisar o novo estado do KCM (atualização "update-code-mode") e adaptar nossos docs aos novos níveis de template. (3) Tratar o rename `CLAUDE.md`→`CEREBRO.md` e decidir se atualizo o CEREBRO inteiro ou passo a tarefa ao Claude Code. (4) Prosseguir com o projeto.

## Feito
- **Busca pelos `.txt`:** varredura em `/mnt/project`, `/mnt/user-data/uploads` (vazio) e todo o ambiente. Os `.txt` citados NÃO existem em lugar nenhum (só os `requirements*.txt` e LICENSEs de libs). Também os arquivos do toolchain da sessão anterior (HUB.md, CLAUDE-HUB*.md, asu-switch.yaml, ideia-260614.txt, console-260614*.txt) saíram do mount — este pacote FlatDrop (21/06) é novo e trouxe os templates `update-code-mode__*`. → Não dá para processá-los; pedido de upload no fim.
- **Confirmação de versão:** o `/mnt/project` JÁ tem minhas edições da sessão 06-19 (CLAUDE.md com seção HUB em 236 linhas, DEC-020, STATUS com Grupo/Toolchain, log 06-19 no pacote). Logo, parti do mount autoritativo, não das saídas antigas (que ficaram superadas).
- **Análise da atualização do KCM:** os templates de doc (STATUS/CONTEXT/DECISIONS/IDEAS/ROADMAP/GLOSSARY/HISTORICO/CHANGELOG/LOG-TEMPLATE) NÃO mudaram de estrutura — são versões em branco do que já usamos; nada a regenerar por template. A mudança real: `CLAUDE.md`(comportamento)→`CEREBRO.md`; novo `CLAUDE.md` curto na raiz p/ o Claude Code; nova seção «Desenvolvimento no Claude Code» (raias chat↔Code, método "doc por spec", `meta/specs/`); apêndice com starter de `.claude/`.
- **Entregue:**
  - `meta/CEREBRO.md` — nosso CLAUDE.md renomeado (6 referências internas trocadas) + nova seção «Desenvolvimento no Claude Code» adaptada ao ASU + seção HUB preservada. NÃO incluí o apêndice de starter (os arquivos foram criados de fato).
  - Arranque na raiz: `CLAUDE.md` (ponteiro curto, ritual + build do ASU), `.claude/settings.json` (permissões: Read/Edit/Grep/Glob, git, `python -m pytest`, `python -m src`, `ruff`, `black`; nega `rm -rf`), `.claude/commands/apply-spec.md` e `.claude/commands/wrap.md` (corrigido p/ `DECISIONS.md`, não `DECISOES.md`).
  - `meta/CONTEXT.md` — rename na listagem de arquivos + bullet do workflow Claude Code.
  - `meta/DECISIONS.md` — DEC-021.
  - `meta/STATUS.md` — seção «Estrutura / Modo Claude Code», backlog de setup, Última Sessão.
  - `meta/IDEAS.md` — Feedback para o Kit (bug de nomes de doc no starter; lembrete do rename nas Instruções do Projeto).

## Decisões
- **DEC-021** (nova): adotar o modo Claude Code; renomear `meta/CLAUDE.md`→`meta/CEREBRO.md` preservando todo o conteúdo (inclui a seção HUB da DEC-020); criar os arquivos de arranque na raiz; não regenerar os demais docs (templates não mudaram).
- **CEREBRO entregue como arquivo inteiro pelo chat** (não passado ao Code via spec): é reescrita FUNDAMENTAL, que o próprio método novo reserva à raia do chat; specs são p/ deltas pequenos em docs grandes; e o Claude Code ainda não estava configurado nesta sessão (bootstrap).

## Bugs
- Nenhum no código (não tocado; segue em 0.6.0, 93 testes verdes).
- **Bug no template do KCM:** o starter de arranque (apêndice do CEREBRO + `wrap.md`) referencia `meta/DECISOES.md`/`REVISOES.md` (nomes do nicho Design), que não existem no nicho Dev (usamos `DECISIONS.md`). Corrigido na adaptação; reportado em IDEAS › Feedback para o Kit.

## Aprendizados / armadilhas
- **Atualização que renomeia arquivo deixa uma ponta solta fora dos arquivos:** as Instruções do Projeto (painel) continuam citando `CLAUDE.md`. O assistente não edita o painel — é ajuste manual do usuário. Sempre sinalizar isso ao aplicar um rename estrutural.
- **Os templates de doc não mudaram** — resistir ao impulso de "atualizar tudo": adaptar = só o que de fato mudou (CEREBRO + modo Code). Regenerar docs idênticos seria churn e risco de perda.
- **Pacote FlatDrop é um instantâneo:** o que não está na pasta nesta volta (toolchain, `.txt`) simplesmente não veio — não assumir que sumiu de propósito; pedir.

## Onde parei
Migração para o modo Claude Code entregue (CEREBRO + arranque + docs atualizados), partindo do mount autoritativo. Código intocado. Próximo passo óbvio: o usuário cria os arquivos no repo, renomeia CLAUDE→CEREBRO em `meta/`, aponta as Instruções do Projeto para CEREBRO, e roda uma sessão de teste do Claude Code.

## Próximos passos
- Criar no repo: `CLAUDE.md` (raiz), `.claude/settings.json`, `.claude/commands/{apply-spec,wrap}.md`; renomear `meta/CLAUDE.md`→`meta/CEREBRO.md`; **apontar as Instruções do Projeto (painel) de CLAUDE.md para CEREBRO.md**.
- Rodar uma sessão de teste do Claude Code (`/wrap`) para validar permissões e build.
- Subir os `.txt` (`260621-Sugestões...`, `260619-ideias.txt`) se quiser que eu os processe (não vieram no pacote).
- Atualizar o status relâmpago do ASU no `HUB.md` (0.6.0). Herdados: README/GUIA 0.6.0; testar o kit/ASU em campo.
- A partir de agora, deltas pequenos em docs grandes podem ir por SPEC em `meta/specs/` p/ o Code aplicar (em vez de o chat reentregar o arquivo inteiro).
