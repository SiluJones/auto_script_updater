# Log — 2026-06-28

## Objetivo da sessão
"Prossiga." Ler 4 arquivos (`260624-2227-code.txt`, `260628-0737.txt`, `Demostração-CEREBRO.md`, `Demostração-instrucoes-dev.txt`). Diagnosticar por que `abrir-asu-fileview.bat` não funciona. Avaliar: 2º botão de .bat (abrir GUI clássico); pedidos do 260628 (backup fora do repo + nome por projeto); a reflexão "ASU para criar arquivo novo é mais caro que entregar o arquivo". Preparar uma mensagem para o KCM sobre o uso do ASU.

## Feito
- **Lidos os arquivos.** `260624-2227-code.txt` = relatório do Claude Code confirmando o endurecimento ASCII (WI-1/2/3) — MAS o código tem a correção e os docs (CHANGELOG/STATUS) NÃO foram reconciliados (estão em 0.7.0 / "[Não lançado]"; o launcher.py já tem `precisa_utf8`/`chcp`). Doc-lag a reconciliar. `260628-0737.txt` = pedidos do usuário (backup externo + nome por projeto; e a observação do projeto que gerou instrução para criar arquivo à mão). `Demostração-instrucoes-dev.txt` = instrução CURTA do painel — NÃO menciona ASU. `Demostração-CEREBRO.md` = CEREBRO com a seção «Saída de código via ASU» (a diretriz do asuMode).
- **Diagnóstico do .bat (causa-raiz):** BUG no `build_launcher_bat` — `--instruction-dir "%~dp0"` termina em `\` → `"...\"` → `\"` lido como aspa escapada → argumento corrompido → resolução pasta→instrução falha. `--root "%~dp0fileview"` não sofre. Correção: `--instruction-dir "%~dp0."`. Bônus: `chcp` deve considerar `bat_dir` (= `%~dp0` em runtime).
- **Análise ASU criar-vs-editar (o "me ilumine"):** o usuário está certo SOBRE ARQUIVO NOVO (instrução embute arquivo inteiro + esqueleto YAML + caminho → mais caro e frágil que entregar para baixar; sem ganho de localização). MAS o caso do `fileview-instrucao.yaml` (mudar 2 chars no deploy.yml) é uso CORRETO do ASU (modificação: instrução pequena, bem menor que reentregar o arquivo). A regra: editar→ASU, criar→baixar (exceto create_file em instrução mista). A causa do "gerou para criar à mão" é a diretriz do KCM, não o ASU.
- **Entregue:**
  - `meta/specs/F2-bat-fix-e-launcher-classico.md` — corrige o BUG do `%~dp0`, melhora o `chcp` (considera `bat_dir`), e adiciona o 2º botão "Criar atalho .bat (abrir GUI)" (`pythonw` + `start /d`, sem console). Manda o Code reconciliar o doc-lag do ASCII em 0.7.1.
  - `meta/specs/F3-backup-na-gui.md` — expõe `--backup-dir` na GUI (recurso já existe no núcleo) e, SÓ quando externo, aninha o backup por `<nome-da-raiz>/` (resolve a "mistura"/nome genérico com 1 nível; MAX_PATH ok porque o espelho já é raso, FIX-008).
  - `kcm/mensagem-para-o-KCM-uso-do-ASU.md` — mensagem para o usuário levar ao chat do KCM: reescrever a diretriz «Saída de código via ASU» (editar→ASU, novo→baixar), levar gatilho de ASU para a instrução curta, `format_version >= 1.0`, lembrar de subir o guia.
  - `meta/IDEAS.md` — capturadas as ideias novas (launcher clássico, bug do .bat, backup na GUI), concluídas as do 0.7.0 (recentes/.bat), e o feedback ao Kit sobre criar-vs-editar.

## Decisões
- **Tudo de código via SPEC (raia do Code), não código no chat** — o repo é dirigido pelo Claude Code; manter o fluxo. Decisões de design (correção do `%~dp0`; `chcp` por `bat_dir`; launcher clássico com `pythonw`/`start /d`; backup externo aninhado por projeto) ficam como DEC a registrar pelo Code ao aplicar.
- **Backup nome-por-projeto: aceito SÓ quando externo** (refutei o caso interno: redundante + MAX_PATH). Aninhar 1 nível em vez de prefixar.

## Bugs
- **BUG real no `build_launcher_bat`** (o `%~dp0` final) — specado para correção.
- **Doc-lag:** o endurecimento ASCII (0.7.1) está no código mas não no CHANGELOG/STATUS — a spec do .bat manda o Code reconciliar.
- **Inconsistência de contagem de testes:** o relatório do Code (06-24) diz 97; o STATUS/0.7.0 diz 112. Sinalizar ao reconciliar (provável base diferente entre relatórios).

## Aprendizados / armadilhas
- **`%~dp0` SEMPRE termina em `\`** — usar `"%~dp0"` cru quebra o argumento (aspa escapada). Sempre `"%~dp0."` ou tirar a barra.
- **ASU: editar≠criar.** O valor do ASU é editar arquivo existente (instrução pequena). Criar arquivo novo via ASU é antipadrão (mais caro/frágil que entregar para baixar), salvo bundle com edições.
- **Regressão recorrente do IDEAS:** pela 3ª vez o IDEAS do pacote voltou ATRÁS das minhas edições (recentes/.bat ainda em «Ativas», não em «Concluídas»). Reapliquei. Há um problema sistemático de o IDEAS não estar sendo aplicado/sincronizado entre turnos — AVISAR o usuário.
- **A instrução curta do painel não cobre ASU** — gatilho no lugar errado; feedback ao KCM.

## Onde parei
Duas specs (correção do .bat + launcher clássico; backup na GUI) e a mensagem para o KCM entregues; IDEAS curado e regressão reaplicada. Código intocado pelo chat. Próximo: rodar as specs no Claude Code; levar a mensagem ao chat do KCM.

## Próximos passos
- Implementar `meta/specs/F2-bat-fix-e-launcher-classico.md` no Code (corrige o .bat + launcher clássico + reconcilia o doc 0.7.1). Testar o .bat numa pasta com 1 yaml e numa pasta com acento.
- Implementar `meta/specs/F3-backup-na-gui.md` (backup-dir na GUI + nome por projeto quando externo).
- Levar `kcm/mensagem-para-o-KCM-uso-do-ASU.md` ao chat do KCM.
- Conferir a regressão do IDEAS (por que não está sendo aplicado entre turnos).
- Pendências herdadas: validação visual da GUI no Windows; `HUB.md` (status relâmpago 0.7.x + `format_version >= 1.0`); README/GUIA; apontar Instruções do Projeto para CEREBRO.
- Confirmar com o usuário os checks do .bat quebrado: yaml na pasta do .bat? subpasta `fileview`? `.venv\Scripts\python.exe` presente?
