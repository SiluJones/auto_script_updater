# Log — 2026-06-15

## Objetivo da sessão
Continuar o desenvolvimento; ler `ideia-260614.txt` (muitas perguntas/ideias) e `console-260614 - 2.txt`; verificar (via `_MANIFEST.md`) se o usuário colocou os arquivos nos lugares certos; pesquisar/analisar/refinar as ideias e corrigir bugs. Ao final, gerar TODOS os arquivos de contexto com máximo detalhe para retomar numa nova conversa sem perda.

## Feito
- **Diagnóstico do `console-260614 - 2.txt`:** BOA NOTÍCIA — tudo funciona no Windows do usuário. `self-test` OK; `validate` OK; `apply --dry-run` e `apply` do `projeto_teste` (saudacao.py → f-string) perfeitos, com backup criado. Não havia bug a corrigir; foi a confirmação de que a v0.5.2 está sólida na máquina dele e que o GUIA_PASSO_A_PASSO funciona ponta a ponta.
- **Processadas as muitas ideias do `ideia-260614.txt`** (todas capturadas em IDEAS): subir PROMPT_IA ao projeto; dúvida sobre local do instrucao.yaml; feedback do ASU vindo de outros projetos; integração de funcionalidades na GUI (faltava sandbox); aplicar em massa/seleção de blocos; gerador de .bat; backup junto da instrução + nome com prefixo da raiz; log/arquivo único de timestamps; opção de não gerar/excluir backup; copiar console (não só erro) e em massa.
- **PESQUISA (3 frentes, para refinar E refutar):**
  - Seleção parcial de mudanças → `git add -p`/`--patch` valida como padrão consagrado (seleção por hunk/arquivo). Mas o objetivo do ASU é atomicidade; seleção fica como opt-in futuro que não quebra o padrão.
  - Nomenclatura de backup → prefixo do projeto + nome ordenável (YYYY-MM-DD) é boa prática; ponto-prefixo é para config e some no Windows. Prefixo da raiz ADIADO (alonga caminho, risco MAX_PATH; só compensa se vários projetos compartilharem UMA pasta de backup).
  - Segurança de auto-aplicar patches de IA → FORTE refutação do .bat que aplica direto: 43% dos patches passam no teste primário mas quebram em casos adversos; tamanho do changeset prediz instabilidade; "human approval mandatory para mudanças destrutivas". Recomendação: .bat de aplicação faz dry-run e PAUSA; .bat de "abrir GUI" é seguro.
- **IMPLEMENTADO (3 features de alto valor/baixo risco), tudo com teste:**
  - **`--backup-dir` (DEC-018):** novo parâmetro `backup_location` em `apply_instruction` (distinto de `root_path`); flag no `apply` e no `rollback`. Cria `backups/` fora do projeto. Testado: projeto fica limpo, rollback acha pela pasta de backup.
  - **`history.log` (DEC-018):** `BackupManager.append_history()` grava uma linha por aplicação (timestamp + nº arquivos + descrição) em `backups/history.log`. CLI imprime o caminho. Testado: acumula linhas, com descrição.
  - **Checkbox de sandbox na GUI (DEC-019):** `make_sandbox` + `SandboxError` + `SANDBOX_IGNORES` MIGRADOS de `__main__.py` para `patch_engine` (core), sinalizando erro por exceção; CLI virou wrapper fino; GUI ganhou o checkbox. Testado offscreen: aplica na cópia, original intocado.
- **Qualidade:** 90 → **93 testes** verdes; ruff/black limpos; versão **0.6.0**; bundle `asu_v0.6.0.zip` validado por extração + pytest (e sem o `health.py` residual).
- **Docs de contexto regenerados ao final (esta entrega):** CONTEXT (reescrito e fiel à realidade — corrigidos arquivos GUI fantasmas, PyInstaller marcado como futuro, CLI atual com 4 comandos), STATUS (0.6.0, máximo detalhe para retomada, com a pendência de README/GUIA explícita), DECISIONS (+DEC-018, +DEC-019), CHANGELOG (0.6.0), IDEAS (8 ideias novas do usuário + 3 concluídas + esclarecimento de feedback do ASU externo), ROADMAP (F2/F3 atualizadas), GLOSSARY (termos novos: history.log, sandbox, backup_location, SandboxError, _MISSING, manifest.txt).

## Decisões
- **DEC-018** — local do backup configurável (`--backup-dir`) + log consolidado (`history.log`). Ver DECISIONS.md.
- **DEC-019** — sandbox movido para o core; checkbox de sandbox na GUI (paridade CLI↔GUI). Ver DECISIONS.md.

## Bugs
- Nenhum bug novo. O `console-260614 - 2.txt` confirmou ausência de erros no fluxo do usuário (os FIX-008 e FIX-009 das sessões anteriores seguem resolvidos).

## Aprendizados / armadilhas
- **Pesquisar antes de aceitar ideia do usuário rende:** a pesquisa transformou "fazer um .bat que aplica direto" (perigoso) em "fazer um .bat com dry-run + pausa" (seguro), e justificou ADIAR o prefixo da raiz no backup (MAX_PATH) — ambos com lastro externo, não só opinião.
- **Lógica compartilhável mora no core:** o sandbox estava preso no `__main__` (com print/SystemExit); movê-lo para o core com exceção destravou a paridade na GUI sem duplicar nada (cumpre DEC-013).
- **CONTEXT tinha "arquivos fantasmas":** citava `file_tree_panel.py`/`diff_viewer.py`/`root_picker.py` que nunca existiram (a GUI é monolítica). Documento estável também apodrece; vale revisar contra a árvore real de vez em quando.

## Onde parei
v0.6.0 fechada e empacotada (`asu_v0.6.0.zip`), 93 testes verdes, confirmada funcionando no Windows. Todos os docs de contexto regenerados para retomada limpa. PENDÊNCIAS explícitas no STATUS/Backlog para a próxima conversa: (1) atualizar README e GUIA_PASSO_A_PASSO para 0.6.0 + correções pedidas; (2) responder formalmente (no chat e/ou implementar) as ideias capturadas que não viraram código (gerador de .bat, copiar saída completa, limpeza de backups, seleção de blocos, registrar rollback no history); (3) o usuário testar a GUI 0.6.0 visualmente no Windows e a feature de campo (kit v2 num projeto consumidor).

## Próximos passos
- [ ] Atualizar README.md (0.6.0: --backup-dir, history.log, checkbox sandbox).
- [ ] Reescrever/atualizar GUIA_PASSO_A_PASSO.md (corrigir confusão do local do .yaml; documentar subir PROMPT_IA ao projeto + referenciar no CLAUDE.md; --backup-dir; history.log; --no-backup; checkbox sandbox).
- [ ] Avaliar/implementar gerador de .bat (com dry-run+pausa) e .bat de abrir GUI; possível botão na GUI.
- [ ] Botão "Copiar saída completa" na GUI (não só erro); avaliar serialização do relatório inteiro.
- [ ] Comando de limpeza de backups antigos (manter últimos N/X dias).
- [ ] Registrar também ROLLBACKS no history.log.
- [ ] Usuário: testar GUI 0.6.0 no Windows (visual/usabilidade) e o kit v2 num projeto consumidor real.
- [ ] Sob demanda: refinamento semântico por família de linguagem (tree-sitter), web > JVM/C-like > nicho.
