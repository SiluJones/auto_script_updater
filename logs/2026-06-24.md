# Log — 2026-06-24

## Objetivo da sessão
"Prossiga." Ler o `.bat` de teste do usuário (`abrir-asu-projeto_teste.bat`) e incorporar o aprendizado que ele trouxe: `.bat` deve ser gerado em ASCII puro. Avançar o projeto.

## Feito
- **Conferido o estado real (princípio 8):** a spec `meta/specs/F2-acesso-rapido.md` JÁ foi implementada pelo Claude Code (commits 06-23). O mount tem: `src/gui/__main__.py` com `argparse` (`--root`/`--instruction-dir`/`--instruction`); `src/gui/main_window.py` com recentes/fixadas (`recent_roots`/`pinned_roots`), `_apply_instruction_dir`, botão "Criar atalho .bat…"; `src/gui/launcher.py` (`resolve_instruction_in_dir` + `build_launcher_bat`); `tests/test_launcher.py`. Versão **0.7.0**, **112 testes** verdes, ruff/black limpos. ROADMAP/STATUS/CHANGELOG já atualizados pelo Code; IDEAS não (Code não mexe em IDEAS).
- **Lido o `.bat` de teste:** `abrir-asu-projeto_teste.bat` é exatamente o template projetado — 100% ASCII, CRLF, sem BOM, usa `%~dp0projeto_teste` (raiz descendente) + `%~dp0` (instrução). Valida o desenho em campo.
- **Aprendizado ASCII analisado:** o conteúdo estático do `.bat` já é ASCII e a escrita usa `encoding="ascii"`. MAS achei dois problemas reais: (1) `errors="replace"` na escrita corromperia SILENCIOSAMENTE um caminho com acento (comum no Windows pt-BR: `Área de Trabalho`, `Café`) — viraria `?` e o `--root` quebraria; (2) não há teste travando a invariante "ASCII quando os caminhos são ASCII" (uma futura edição que reponha um em-dash regrediria sem aviso).
- **Entregue — spec `meta/specs/F2-bat-ascii.md`** (para o Claude Code): caminhos ASCII → `.bat` ASCII puro (sem `errors="replace"`); algum caminho não-ASCII → `chcp 65001 >nul` + arquivo UTF-8 SEM BOM (forma reconhecida de o CMD resolver acento); nunca corromper em silêncio; + testes (ASCII → `.isascii()` e sem `chcp`; acento → contém `chcp 65001` e o nome intacto). A spec manda o Code registrar DEC-023, bump 0.7.1 e atualizar STATUS/CHANGELOG ao concluir.
- **IDEAS:** movidas para «Concluídas» as duas ideias entregues em 0.7.0 (recentes/fixadas; atalho .bat + args), com nota da validação de campo e referência ao endurecimento ASCII.

## Decisões
- **Endurecimento ASCII via SPEC (raia do Code), não código no chat.** O repo agora é dirigido pelo Claude Code; largar arquivos `.py` editados pelo chat criaria duas fontes de verdade a reconciliar. A spec entra limpa no fluxo que já funcionou (06-23). Decisão de DESIGN do `.bat` (ASCII × UTF-8+chcp) fica como DEC-023, a ser registrada pelo Code ao aplicar.
- **Não regenerei STATUS/CHANGELOG/ROADMAP/DECISIONS:** estão atualizados para 0.7.0 pelo Code e serão tocados de novo pelo Code ao aplicar a spec ASCII (um canal por doc por ciclo — evita conflito). IDEAS é chat-lane (Code não mexe), então foi curado aqui.

## Bugs
- Nenhum quebrado. Latente pego e specado: `errors="replace"` corromperia caminho acentuado no `.bat` (será resolvido pela spec ASCII).

## Aprendizados / armadilhas
- **`.bat` no CMD:** ASCII puro quando der; para caminho com acento, `chcp 65001` + UTF-8 **sem BOM** (BOM no `.bat` quebra a 1ª linha). `errors="replace"` ao gravar é perigoso: mascara o problema corrompendo o caminho.
- **O fluxo spec→Claude Code funcionou ponta a ponta** (autoria no chat → implementação + testes + docs pelo Code → validação de campo pelo usuário). Confirmado como o modo de trabalho do projeto para código.
- **Conferir o estado real antes de "prosseguir":** a feature já estava pronta; a tarefa real era o refinamento de ASCII, não reimplementar.

## Onde parei
Spec do endurecimento ASCII entregue para o Claude Code; IDEAS curado (duas ideias concluídas). Código intocado pelo chat. Próximo passo óbvio: rodar `/apply-spec F2-bat-ascii.md` no Claude Code; ele aplica, roda os testes, registra DEC-023, bumpa para 0.7.1 e atualiza STATUS/CHANGELOG.

## Próximos passos
- Implementar `meta/specs/F2-bat-ascii.md` no Claude Code (WI-1..WI-3) e validar com `pytest` + gerar um `.bat` para uma raiz acentuada de teste.
- Validação VISUAL da GUI no Windows (os botões novos de recentes/fixadas e "Criar atalho .bat…") — pendência herdada do 06-23.
- Arquivar os dois `.txt` já processados; apontar as Instruções do Projeto (painel) para `CEREBRO.md`.
- Herdados: `HUB.md` (status relâmpago 0.7.0 + ancorar diretriz no `format_version >= 1.0`); README/GUIA.
- Próximo increment da F2 quando quiser: barra de progresso, tema claro/escuro, syntax highlight no diff, desfazer timestamps antigos.
